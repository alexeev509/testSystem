Задача 1
Проанализировать алгоритм и определить его результат
Алгоритм
1. Создается таймер:
 timer = CreateWaitableTimer (NULL, TRUE, NULL)
2. Вводятся целочисленные значения h и time:
 h = 10000000;
 time = – 5 * h
3. Настраивается таймер:
 SetWaitableTimer (timer, &time, 0, NULL, NULL, FALSE)

Результат (когда и сколько раз сработает таймер):
_____
Задача 2
Проанализировать алгоритм и определить его результат

Алгоритм
1. Создается таймер:
 timer = CreateWaitableTimer (NULL, TRUE, NULL)
2. Вводятся целочисленные значения h и time:
 h = 100000000;
 time = – 10 * h
3. Настраивается таймер:
 SetWaitableTimer (timer, &time, 2000, NULL, NULL, FALSE)

Результат (когда и сколько раз сработает таймер):
_____
Задача 3
Проанализировать алгоритм и определить его результат

Алгоритм
1. Создается таймер:
 timer = CreateWaitableTimer (NULL, TRUE, NULL)
2. Вводятся целочисленные значения h и time:
 h = 100000000;
 time = 5 * h
3. Настраивается таймер:
 SetWaitableTimer (timer, &time, 0, NULL, NULL, FALSE)

Результат (когда и сколько раз сработает таймер):
_____
Задача 4
Проанализировать алгоритм и определить его результат

Алгоритм
1. Создается таймер:
 timer = CreateWaitableTimer (NULL, TRUE, NULL)
2. Вводятся целочисленные значения h и time:
 h = 100000000;
 time = 10 * h
3. Настраивается таймер:
 SetWaitableTimer (timer, &time, 2000, NULL, NULL, FALSE)

Результат (когда и сколько раз сработает таймер):
_____
Задача 5
Проанализировать алгоритм и определить его результат

Алгоритм
1. Создается таймер:
 timer = CreateWaitableTimer (NULL, TRUE, NULL)
2. Вводятся целочисленные значения h и time:
 h = 100000000;
 time = – 5 * h
3. Настраивается таймер:
 SetWaitableTimer (timer, &time, –2000, NULL, NULL, FALSE)

Результат (когда и сколько раз сработает таймер):